{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-workspace",
  "description": "Grants the embedded pear web app (a REMOTE origin the main window navigates to) the minimal IPC surface for the engine manager. Deliberately narrow: events + our own commands, no fs/shell/window permissions. Self-hosted domains outside these patterns need a rebuild with an added URL (see desktop/README.md).",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://localhost:*",
      "http://127.0.0.1:*",
      "https://*.pear.pro"
    ]
  },
  "permissions": [
    "core:default",
    "allow-engines-detect",
    "allow-engine-bind",
    "allow-engines-bindings",
    "allow-session-start",
    "allow-session-resume",
    "allow-session-send",
    "allow-session-cancel",
    "allow-sessions-list",
    "allow-session-meta",
    "allow-bridge-local-pair-prepare",
    "allow-bridge-local-start",
    "allow-bridge-local-stop",
    "allow-bridge-local-status"
  ]
}
