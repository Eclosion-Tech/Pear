{
  "$comment": "Canonical table policy for the pear-snapshot-v2 export/import format. Every PUBLIC module table must appear in exactly one of `include` or `exclude` \u2014 enforced from the TypeScript side (pear/web/src/lib/pearExport.test.ts, against generated bindings) and the Rust side (pear/server/spacetimedb/src/import/pear_v2.rs tests, against the importer dispatch table). Adding a module table without updating this file fails CI on both sides. Private tables are not in client bindings and are intentionally outside this policy (documented in docs/RUNBOOKS/restore-workspace.md).",
  "format": "pear-snapshot-v2",
  "include": [
    "user",
    "user_preference",
    "page",
    "page_content",
    "page_yjs_state",
    "page_snapshot",
    "component_node",
    "component_yjs_state",
    "component_type_definition",
    "database_schema",
    "property_definition",
    "database_view",
    "page_property_value",
    "page_property_value_history",
    "database_row_marker",
    "attachment",
    "conversation_attachment",
    "page_access_rule",
    "block_access_rule",
    "page_access_request",
    "ai_user_profile",
    "ai_user_memory",
    "ai_user_routine",
    "ai_evaluation",
    "conversation",
    "conversation_participant",
    "conversation_message",
    "message_feedback",
    "harness_template",
    "review_agent_binding",
    "review_annotation",
    "auto_apply_binding",
    "extension_manifest",
    "installed_extension",
    "orcha_agent",
    "orcha_job",
    "orcha_task",
    "orcha_shared_context",
    "orcha_usage_event",
    "api_endpoint",
    "api_field_mapping",
    "api_endpoint_key",
    "api_call_log",
    "automation_rule",
    "automation_action",
    "automation_condition",
    "automation_capability",
    "automation_event_queue",
    "automation_run_log",
    "bridge_command",
    "bridge_command_result",
    "bridge_device_allowlist",
    "bridge_device_grant",
    "bridge_device_summary",
    "structural_sensor_finding"
  ],
  "exclude": {
    "ai_extension_runtime": "Caller-scoped runtime view containing MCP endpoints and credentials. It is derived from installed extension configuration and must never be exported.",
    "ai_user_config": "Contains plaintext provider API keys and worker tokens. Never exported. Import restores stub configs from ai_user_profile rows (same behavior as v1); operators reconfigure keys post-restore.",
    "automation_primitive": "Seeded catalog of available primitives keyed by name (seed_automation_primitives_inner); module-defined, not user data.",
    "extension_runtime_health": "Transient worker handshake and tool-discovery status. It is re-created by AI workers after a restore.",
    "id_counter": "Module-managed id allocator. import_v2_commit deletes all counter rows so the next allocation re-seeds from post-import max(id) (see id_counters.rs).",
    "migration_state": "Module-managed migration bookkeeping; owned by the module version that publishes, never restored.",
    "module_install_meta": "Module-managed publisher-identity record; re-created by init on the restore target.",
    "notion_import_job": "Transient background-import queue (carries Notion token ciphertexts, blanked on completion); jobs are meaningless on a restore target and must never ride along in backups.",
    "sensor_registry": "Fully seeded at init (seed_sensor_registry_inner); findings reference sensors by string (sensor_kind, code), never by id, so re-seeded rows are equivalent."
  },
  "import_row_guards": {
    "harness_template": "Skip rows with source == Builtin (re-seeded by init; unique external_id would collide).",
    "extension_manifest": "Skip rows with extension_type == Builtin (seeded by seed_builtin_extensions_inner; ids collide with counter-allocated seeds).",
    "installed_extension": "Skip rows whose manifest_id refers to a skipped Builtin manifest (the seed creates its own installed row).",
    "component_type_definition": "Skip rows with is_builtin == true (re-seeded by init; component_node references types by string, so id drift is harmless)."
  }
}
