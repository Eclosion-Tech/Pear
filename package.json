{
  "name": "pear",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "generate": "pnpm --filter web generate"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  }
}
